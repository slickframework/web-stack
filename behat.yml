# Behat configuration file
default:
  suites:
    default:
      contexts:
        - FeatureContext
      paths:
        - %paths.base%/features
  extensions:
    Behat\MinkExtension:
      base_url: 'http://php'
      sessions:
        default:
          goutte: ~