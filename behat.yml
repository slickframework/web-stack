# Behat configuration file
default:
  suites:
    default:
      contexts:
        - FeatureContext
        - CommandLine\CommandLineContext
      paths:
        - %paths.base%/features
  extensions:
    Behat\MinkExtension:
      base_url: 'http://0.0.0.0:8880'
      sessions:
        default:
          goutte: ~