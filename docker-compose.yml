version: '2'

services:

  php:
    build: docker/php
    ports:
      - 80
    volumes:
      - ./:/var/www/app
    environment:
      - USE_XDEBUG=yes

  docs:
    image: nginx:latest
    ports:
      - 80
    volumes:
      - ./docs/build:/usr/share/nginx/html:ro