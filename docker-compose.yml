services:

  php:
    build: docker/php
    volumes:
      - ./:/var/www/app
    ports:
      - 8080:80
    environment:
      - USE_XDEBUG=yes
      - XDEBUG_MODE=develop,debug,coverage
      - ENABLE_OPCACHE=no
      - ENABLE_XDEBUG_FOR_MAC=yes
