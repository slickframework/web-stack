routes:

    article.read:
        path: "/article/{date}/{slug}-{id}"
        method: GET
        defauts:
            namespace: Articles\Controller
            action: handle
            controller: article-read

    errors: articles/errors
